# Backend for FableSmithStudio Website
This folder contains the Express.js backend for the MERN stack application.

## Setup
- Install dependencies: `npm install`
- Start server: `npm run dev` or `npm start`
